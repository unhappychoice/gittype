use crate::Result;

pub struct GameDisplay;

impl GameDisplay {
    pub fn new() -> Self {
        Self
    }

    pub fn render(&self) -> Result<()> {
        // TODO: Implement game display rendering
        Ok(())
    }
}